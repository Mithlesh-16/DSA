#include <bits/stdc++.h>
using namespace std;

//trying to detect the bridge in graph 
//using the data structure low, discovery and visited
int bridge_in_graph(vector<int> &discovery, vector<int> &lower, vector<int> &visited){

}


int main() {

    vector<vector<int>> edges;
    vector<int> discovery, lower, visisted;
    for(int i = 0; i < edges.size(); i++){

        

    }

    // bridge_in_graph(Discovery, lower, visited);

return 0;
}